# ==============================================================================
# PRODUCTION ENVIRONMENT VARIABLES FOR DOCKER COMPOSE
# ==============================================================================
# Copy this file to .env in the root directory and update all values
# IMPORTANT: Generate secure random values for all secrets!
# ==============================================================================

# ------------------------------------------------------------------------------
# PostgreSQL Database Configuration
# ------------------------------------------------------------------------------
POSTGRES_USER=chikox_user
POSTGRES_PASSWORD=change_this_strong_password_123_generate_with_openssl
POSTGRES_DB=chikox

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# ------------------------------------------------------------------------------
# JWT Configuration
# ------------------------------------------------------------------------------
# Generate secure secrets with: openssl rand -base64 32
JWT_ACCESS_SECRET=change_this_to_random_string_min_32_chars_use_openssl_rand
JWT_REFRESH_SECRET=change_this_to_random_string_min_32_chars_use_openssl_rand
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ------------------------------------------------------------------------------
# CORS Configuration
# ------------------------------------------------------------------------------
# Update with your actual frontend domains (comma-separated, no spaces)
CORS_ORIGIN=http://your-domain.com,http://admin.your-domain.com

# ------------------------------------------------------------------------------
# Cookie Configuration
# ------------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
COOKIE_SECRET=change_this_to_random_string_min_32_chars_use_openssl_rand

# ------------------------------------------------------------------------------
# Database URL for Prisma
# ------------------------------------------------------------------------------
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# In Docker Compose, use 'database' as the host (service name)
DATABASE_URL=postgresql://chikox_user:change_this_strong_password_123@database:5432/chikox

# ------------------------------------------------------------------------------
# Client Environment Variables
# ------------------------------------------------------------------------------
# Update with your actual API domain
NEXT_PUBLIC_API_URL=http://your-domain.com

# ------------------------------------------------------------------------------
# Admin Environment Variables
# ------------------------------------------------------------------------------
# Update with your actual API domain
VITE_API_URL=http://your-domain.com

# ==============================================================================
# QUICK SETUP COMMANDS
# ==============================================================================
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Generate secrets automatically:
#    JWT_ACCESS_SECRET=$(openssl rand -base64 32)
#    JWT_REFRESH_SECRET=$(openssl rand -base64 32)
#    COOKIE_SECRET=$(openssl rand -base64 32)
#    POSTGRES_PASSWORD=$(openssl rand -base64 24)
#
# 3. Update DATABASE_URL with the generated POSTGRES_PASSWORD
#
# 4. Update CORS_ORIGIN, NEXT_PUBLIC_API_URL, and VITE_API_URL with your domains
#
# 5. Start Docker Compose:
#    docker-compose up -d --build
# ==============================================================================
